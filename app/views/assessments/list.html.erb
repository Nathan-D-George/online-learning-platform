<div class='d-flex justify-content-center align-items-center'>
  <div class='col-md-6'>
    <h2> Quizzes </h2>
    <% @quizzes.each do |quiz| %>
      <p> <%= link_to quiz.name, take_quiz_path(id: quiz.id), class:'mt-3', style:'color: black' %> <p>
    <% end %>
  </div>
</div>