<div class='d-flex align-items-center justify-content-center'>
  <div class='col-md-6'>
    <h1> <%= @room.name %> </h1>
    <%= turbo_stream_from "messages" %>
    <div class='chatroom' id='messages'>
      <%= render @messages %>
    </div>
    <%= render 'messages/new_message_form' %>
  </div>
</div>